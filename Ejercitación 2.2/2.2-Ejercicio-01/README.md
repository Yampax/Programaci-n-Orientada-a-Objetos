n este ejercicio se define la clase ArticuloGeek con los atributos nombre y precioBase, que permiten representar las características básicas de un artículo. Luego, en el método main, se crean dos objetos diferentes mediante new, demostrando la instanciación de una clase.

Los valores de cada objeto se asignan directamente utilizando el operador de acceso punto (.), tal como solicita la consigna. Finalmente, se muestran por consola los datos de ambas instancias, comprobando que cada objeto mantiene sus propios valores de manera independiente.

<img width="1271" height="652" alt="image" src="https://github.com/user-attachments/assets/38d2b096-ee81-4b40-96ba-7336e4d4a9e8" />
